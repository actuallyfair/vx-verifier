syntax = "proto3";

enum Currency {
  CURRENCY_UNSPECIFIED = 0;
  BTC = 1;  // Bitcoin. Provide the value in full bitcoin, not satoshis.
  TBTC = 2; // Test bitcoin. Provide the value in full bitcoin, not satoshis.
  ETH = 3; // Ethereum. Provide the value in full Ethereum
}